console.log("Types of operators in JS:")
let a = 50;
let b = 5;
console.log("a + b: ", a+b)
console.log("a - b: ", a-b)
console.log("a * b: ", a*b)
console.log("a / b: ", a/b)

console.log("a % b: ", a%b) // Modulus same as in python
console.log("a++: ", a++) // will print a but next time a is used a is incremented
console.log("a is now: ",a)
console.log("++a: ", ++a)  // will print a+1 directly (increment is before_) (now a of a++ is used )
// As you go further value of variables keep changing ... Dynamic Programming Language)

console.log(a == b)
console.log(a!=b) // Same as in python 

console.log(a === b)
console.log("\n\n\n\n")
let y = 45; // Number
let z = "45"; // String
console.log(y == z) // double equals equal value and equal type 
console.log(y === z ) // triple equals equal value only

// logical operators perform on boolean
// and operator in python == &&
// or operator in python == ||
// not operator in python ==  !

let w = 89;
let u = 98;
console.log(w>u || w == u) // boolean values will be returned .. because both are wrong itll result false
console.log(w<u && w!=u) // true will return because both are correct